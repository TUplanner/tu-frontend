import FormRedirector from "@/components/FormRedirector";

const Courses = () => {
  return (
    <FormRedirector>
      <p>Program:</p>
      <p>Termm:</p>
    </FormRedirector>
  );
};

export default Courses;
